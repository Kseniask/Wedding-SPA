import { Modal } from 'react-bootstrap';
import { styled } from 'styled-components';

const StModalHeader = styled(Modal.Header)`
  justify-content: center;

  #language-selection-title {
    font-weight: unset;
  }
`;
export default StModalHeader;
